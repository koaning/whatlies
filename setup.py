from setuptools import setup, find_packages

setup(
    name='whatlies',
    version='0.1.0',
    install_requires=[
        'numpy',
        'matplotlib',
        'spacy',
        'mkdocs-jupyter'
    ]
)
